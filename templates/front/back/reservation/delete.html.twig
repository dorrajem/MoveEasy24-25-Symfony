<form method="post" action="{{ path('app_reservation_delete', {'id': reservation.idR}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette reservation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.idR) }}">
    <button class="btn btn-danger btn-sm" type="submit">
    <i class="fas fa-trash-alt"></i>Supprimer
    </button>
</form>
