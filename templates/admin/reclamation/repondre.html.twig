<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Répondre à la réclamation #{{ reclamation.id }}</title>
    <style>
        :root {
            --primary-color: #0B5351;
            --primary-medium: #00A896;
            --primary-light: #99D5CF;
            --secondary-color: #0B5351;
            --accent-color: #F3752B;
            --success-color: #10B981;
            --warning-color: #F59E0B;
            --danger-color: #EF4444;
            --info-color: #3B82F6;
            --text-dark: #1F2937;
            --text-medium: #4B5563;
            --text-light: #F9FAFB;
            --background-light: #F9FAFB;
            --background-medium: #E5E7EB;
            --background-dark: #111827;
            --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --card-shadow-hover: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.06);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--background-light);
            color: var(--text-dark);
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        h1 {
            color: var(--primary-color);
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--primary-light);
            display: flex;
            align-items: center;
        }

        h1::before {
            content: "";
            display: inline-block;
            width: 24px;
            height: 24px;
            background-image: url('/api/placeholder/24/24');
            margin-right: 12px;
        }

        .card {
            background-color: white;
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            margin-bottom: 20px;
            transition: box-shadow 0.3s ease;
        }

        .card:hover {
            box-shadow: var(--card-shadow-hover);
        }

        .card-header {
            background-color: var(--primary-light);
            color: var(--primary-color);
            padding: 15px 20px;
            border-radius: 8px 8px 0 0;
            font-weight: 600;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .card-header .badge {
            background-color: var(--primary-color);
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
        }

        .card-body {
            padding: 20px;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }

        .info-item {
            background-color: var(--background-light);
            padding: 10px 15px;
            border-radius: 6px;
            border-left: 3px solid var(--primary-medium);
        }

        .description-box {
            background-color: var(--background-light);
            padding: 20px;
            border-radius: 6px;
            margin-bottom: 20px;
            border-left: 3px solid var(--accent-color);
        }

        .separator {
            height: 1px;
            background-color: var(--background-medium);
            margin: 25px 0;
        }

        .form-label {
            color: var(--text-dark);
            font-weight: 500;
            margin-bottom: 8px;
            display: block;
        }

        .form-control {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid var(--background-medium);
            border-radius: 6px;
            font-size: 1rem;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary-medium);
            box-shadow: 0 0 0 3px var(--primary-light);
        }

        .btn {
            padding: 10px 16px;
            border: none;
            border-radius: 6px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.1s;
        }

        .btn:active {
            transform: translateY(1px);
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
        }

        .btn-primary:hover {
            background-color: var(--primary-medium);
        }

        .btn-secondary {
            background-color: var(--background-medium);
            color: var(--text-medium);
        }

        .btn-secondary:hover {
            background-color: var(--text-medium);
            color: var(--background-light);
        }

        .action-buttons {
            display: flex;
            gap: 10px;
            margin-top: 20px;
        }

        .status-tag {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            background-color: var(--warning-color);
            color: white;
        }

        .user-info {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: var(--primary-light);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 10px;
            color: var(--primary-color);
            font-weight: bold;
        }

        @media (max-width: 768px) {
            .info-grid {
                grid-template-columns: 1fr;
            }
            
            .action-buttons {
                flex-direction: column;
            }
            
            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>
            Répondre à la réclamation #{{ reclamation.id }}
            <span class="status-tag">En attente</span>
        </h1>
        
        <div class="card">
            <div class="card-header">
                <div>Détails de la réclamation</div>
                <div class="badge">{{ reclamation.categorie }}</div>
            </div>
            <div class="card-body">
                <div class="user-info">
                    <div class="user-avatar">
                        {{ reclamation.idUtilisateur|first|upper }}
                    </div>
                    <div>
                        <strong>Utilisateur ID:</strong> {{ reclamation.idUtilisateur }}
                    </div>
                </div>
                
                <div class="info-grid">
                    <div class="info-item">
                        <strong>Date de soumission:</strong><br>
                        {{ reclamation.dateSoumission|date('d/m/Y H:i') }}
                    </div>
                    <div class="info-item">
                        <strong>Dernière mise à jour:</strong><br>
                        {{ reclamation.dateSoumission|date('d/m/Y H:i') }}
                    </div>
                </div>
                
                <div class="description-box">
                    <h3>Description</h3>
                    <p>{{ reclamation.description }}</p>
                </div>
                
                <div class="separator"></div>
                
                <form method="post" id="reponseForm">
                    <div class="mb-3">
                        <label for="reponse" class="form-label">Votre réponse</label>
                        <textarea class="form-control" id="reponse" name="reponse" rows="5" required placeholder="Saisissez votre réponse ici..."></textarea>
                    </div>
                    
                    <div class="action-buttons">
                        <button type="submit" class="btn btn-primary">
                            Envoyer la réponse
                        </button>
                        <a href="{{ path('admin_reclamation_index') }}" class="btn btn-secondary">
                            Annuler
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Animation d'apparition
            document.querySelector('.card').classList.add('fade-in');
            
            // Validation du formulaire
            document.getElementById('reponseForm').addEventListener('submit', function(e) {
                const reponseText = document.getElementById('reponse').value.trim();
                if (reponseText.length < 10) {
                    e.preventDefault();
                    alert('Votre réponse doit contenir au moins 10 caractères.');
                }
            });

            // Focus automatique sur le champ de réponse
            document.getElementById('reponse').focus();
        });
    </script>
</body>
</html>